import React, { useEffect, useState } from 'react';
import { createRoot } from 'react-dom/client';
import './styles.css';
import { supabase } from './supabase';
import VehicleTypes from './modules/vehicles/VehicleTypes';
import { getVehicleTypes } from './modules/vehicles/vehicleService';
import { saveWaybill } from './modules/waybills/waybillService';
import WaybillList from './modules/waybills/WaybillList';
import TripList from './modules/waybills/TripList';

const num = v => Number(String(v).replace(',', '.').replace(/[^0-9.]/g, '')) || 0;
const sum = v => String(v).trim().split(/\s+/).filter(Boolean).reduce((a, x) => a + num(x), 0);
const fmt = n => Number(n || 0).toLocaleString('ru-RU', { maximumFractionDigits: 2 });
const normalizePhone = value => { let d = String(value).replace(/\D/g, ''); if (d.startsWith('8')) d = '7' + d.slice(1); if (!d.startsWith('7') && d.length) d = '7' + d; return d.slice(0, 11); };
const phoneToLogin = value => `${normalizePhone(value)}@putevka.local`;
const formatPhone = value => { const d = normalizePhone(value); if (!d) return ''; let r = '+' + d[0]; if (d.length > 1) r += d.slice(1,4); if (d.length > 4) r += d.slice(4,7); if (d.length > 7) r += d.slice(7,9); if (d.length > 9) r += d.slice(9,11); return r; };

function NumericInput({ value, onChange, onEnter, placeholder = '', decimal = false, autoFocus = false }) {
  const apply = e => { if (e.key === 'Enter') { e.preventDefault(); onEnter?.(); e.currentTarget.blur(); } };
  return <input autoFocus={autoFocus} inputMode={decimal ? 'decimal' : 'numeric'} pattern={decimal ? '[0-9,. ]*' : '[0-9 ]*'} type="text" enterKeyHint="done" placeholder={placeholder} value={value} onChange={onChange} onKeyDown={apply} />;
}

function Auth({ onLogin }) {
  const [phone,setPhone]=useState(''),[password,setPassword]=useState(''),[loading,setLoading]=useState(false),[error,setError]=useState('');
  const login=async()=>{setError('');const p=normalizePhone(phone);if(p.length!==11||!p.startsWith('7'))return setError('Введите номер телефона в формате +79271234567');setLoading(true);const{data,error}=await supabase.auth.signInWithPassword({email:phoneToLogin(p),password});setLoading(false);if(error)return setError(error.message==='Invalid login credentials'?'Неверный номер телефона или пароль':error.message);onLogin(data.user)};
  return <main className="app auth-page"><section className="card auth-card"><h1>Путёвка</h1><p>Вход в приложение</p><label>Номер телефона</label><input type="tel" inputMode="tel" autoComplete="username" value={formatPhone(phone)} onChange={e=>setPhone(e.target.value)} onKeyDown={e=>e.key==='Enter'&&login()} placeholder="+79271234567"/><label>Пароль</label><input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>e.key==='Enter'&&login()} placeholder="Введите пароль"/>{error&&<div className="error">{error}</div>}<button className="primary big" disabled={loading||normalizePhone(phone).length!==11||!password} onClick={login}>{loading?'Вход...':'Войти'}</button></section></main>;
}

function Admin({ profile, onBack }) {
  const [tab,setTab]=useState('drivers');
  const [drivers,setDrivers]=useState([]);
  const [loading,setLoading]=useState(false);
  const [error,setError]=useState('');
  const [showAdd,setShowAdd]=useState(false);
  const [topupDriver,setTopupDriver]=useState(null);
  const [topupAmount,setTopupAmount]=useState('');
  const [topupDescription,setTopupDescription]=useState('');
  const [phone,setPhone]=useState(''),[name,setName]=useState(''),[password,setPassword]=useState('');
  const loadDrivers=async()=>{setLoading(true);setError('');const{data,error}=await supabase.from('profiles').select('id,full_name,role,is_active,balance').eq('role','driver').order('full_name');if(error)setError(error.message);else setDrivers(data||[]);setLoading(false)};
  useEffect(()=>{loadDrivers()},[]);
  const createDriver=async()=>{setError('');if(!name.trim())return setError('Введите ФИО');if(normalizePhone(phone).length!==11)return setError('Введите номер телефона в формате +79271234567');if(password.length<6)return setError('Пароль должен быть не менее 6 символов');setLoading(true);const{data,error}=await supabase.functions.invoke('create-driver',{body:{phone,full_name:name,password}});if(error)setError(error.message);else if(data?.error)setError(data.error);else{setShowAdd(false);setName('');setPhone('');setPassword('');await loadDrivers()}setLoading(false)};
  const topUp=async()=>{setError('');const amount=num(topupAmount);if(!topupDriver)return;if(!amount||amount<=0)return setError('Введите сумму пополнения');setLoading(true);const{data,error}=await supabase.functions.invoke('manage-balance',{body:{driver_id:topupDriver.id,amount,description:topupDescription.trim()||'Пополнение баланса'}});if(error)setError(error.message);else if(data?.error)setError(data.error);else{setTopupDriver(null);setTopupAmount('');setTopupDescription('');await loadDrivers()}setLoading(false)};
  return <main className="app"><header><div><h1>Администратор</h1><p>Управление</p></div><button className="icon" onClick={onBack}>×</button></header><div className="admin-tabs"><button className={tab==='drivers'?'active':''} onClick={()=>setTab('drivers')}>👨‍✈️ Водители</button><button className={tab==='vehicles'?'active':''} onClick={()=>setTab('vehicles')}>🚗 Автомобили</button></div>{tab==='drivers'&&<><section className="card"><div className="row"><h2>Водители</h2><button className="small" onClick={()=>setShowAdd(true)}>＋ Добавить</button></div>{error&&<div className="error">{error}</div>}{loading&&!drivers.length?<p className="muted">Загрузка...</p>:!drivers.length?<p className="muted">Водителей пока нет.</p>:drivers.map(d=><div className="driver-item" key={d.id}><div><b>{d.full_name}</b><span>{d.is_active?'Активен':'Заблокирован'}</span></div><div className="driver-balance"><strong>{fmt(d.balance)} ₽</strong><button className="small" onClick={()=>{setError('');setTopupDriver(d);setTopupAmount('');setTopupDescription('')}}>＋ Пополнить</button></div></div>)}</section></>}{tab==='vehicles'&&<VehicleTypes/>}{showAdd&&<div className="modal"><div className="modal-card"><div className="row"><h2>Новый водитель</h2><button className="icon" onClick={()=>setShowAdd(false)}>×</button></div><label>ФИО</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="Иванов Иван Иванович"/><label>Номер телефона</label><input type="tel" inputMode="tel" value={formatPhone(phone)} onChange={e=>setPhone(e.target.value)} placeholder="+79271234567"/><label>Пароль</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Минимум 6 символов" onKeyDown={e=>e.key==='Enter'&&createDriver()}/>{error&&<div className="error">{error}</div>}<button className="primary" disabled={loading} onClick={createDriver}>{loading?'Создание...':'Создать водителя'}</button></div></div>}{topupDriver&&<div className="modal"><div className="modal-card"><div className="row"><h2>Пополнение баланса</h2><button className="icon" onClick={()=>setTopupDriver(null)}>×</button></div><p><b>{topupDriver.full_name}</b></p><p>Текущий баланс: <b>{fmt(topupDriver.balance)} ₽</b></p><label>Сумма, ₽</label><NumericInput autoFocus placeholder="1000" value={topupAmount} onChange={e=>setTopupAmount(e.target.value)} onEnter={topUp}/><label>Комментарий</label><input value={topupDescription} onChange={e=>setTopupDescription(e.target.value)} placeholder="Например: пополнение" onKeyDown={e=>e.key==='Enter'&&topUp()}/>{error&&<div className="error">{error}</div>}<button className="primary" disabled={loading} onClick={topUp}>{loading?'Пополнение...':'Пополнить'}</button></div></div>}</main>;
}

function App({ user, profile, onLogout }) {
  const [screen,setScreen]=useState('home');
  const [vehicleTypes,setVehicleTypes]=useState([]);
  const [vehicleLoading,setVehicleLoading]=useState(true);
  const [carId,setCarId]=useState('');
  const [odometer,setOdometer]=useState(''),[startFuel,setStartFuel]=useState(''),[odometerLocked,setOdometerLocked]=useState(false),[startFuelLocked,setStartFuelLocked]=useState(false),[trips,setTrips]=useState([]),[refuels,setRefuels]=useState([]),[city,setCity]=useState(''),[highway,setHighway]=useState(''),[refuel,setRefuel]=useState(''),[showTrip,setShowTrip]=useState(false),[showRefuel,setShowRefuel]=useState(false),[showFinish,setShowFinish]=useState(false);
  const [saveLoading,setSaveLoading]=useState(false),[saveError,setSaveError]=useState('');
  useEffect(()=>{let cancelled=false;getVehicleTypes({activeOnly:true}).then(items=>{if(cancelled)return;setVehicleTypes(items);setCarId(prev=>prev&&items.some(x=>x.id===prev)?prev:(items[0]?.id||''));}).catch(()=>{if(!cancelled)setVehicleTypes([])}).finally(()=>{if(!cancelled)setVehicleLoading(false)});return()=>{cancelled=true};},[]);
  const car=vehicleTypes.find(x=>x.id===carId);
  const norm=car||{city_consumption:0,highway_consumption:0,name:'Автомобиль'};
  const cityKm=sum(city),highwayKm=sum(highway),draftFuel=cityKm*Number(norm.city_consumption)/100+highwayKm*Number(norm.highway_consumption)/100,usedFuel=trips.reduce((a,x)=>a+x.fuel,0),addedFuel=refuels.reduce((a,x)=>a+x,0),tripKm=trips.reduce((a,x)=>a+x.city+x.highway,0),currentOdometer=num(odometer)+tripKm,remaining=Math.max(0,num(startFuel)+addedFuel-usedFuel),lowFuel=remaining<=10;
  const addTrip=()=>{if(!cityKm&&!highwayKm)return;setTrips([...trips,{city:cityKm,highway:highwayKm,fuel:draftFuel}]);setCity('');setHighway('');setShowTrip(false)};
  const addRefuel=()=>{const litres=num(refuel);if(!litres)return;setRefuels([...refuels,litres]);setRefuel('');setShowRefuel(false)};
  const deleteTrip=index=>setTrips(prev=>prev.filter((_,i)=>i!==index));
  const resetWaybillForm=()=>{setOdometer('');setStartFuel('');setOdometerLocked(false);setStartFuelLocked(false);setTrips([]);setRefuels([]);setCity('');setHighway('');setRefuel('');setShowTrip(false);setShowRefuel(false);setShowFinish(false);setSaveError('');};
  const finishWaybill=async()=>{setSaveError('');if(!carId)return setSaveError('Не выбран автомобиль');if(!num(odometer))return setSaveError('Введите одометр при выезде');if(!num(startFuel)&&num(startFuel)!==0)return setSaveError('Введите начальный остаток топлива');setSaveLoading(true);try{await saveWaybill({driverId:user.id,vehicleTypeId:carId,startOdometer:num(odometer),endOdometer:currentOdometer,startFuel:num(startFuel),fuelUsed:usedFuel,fuelAdded:addedFuel,endFuel:remaining,totalKm:tripKm,trips,refuels});resetWaybillForm();alert('Путёвка сохранена');}catch(error){setSaveError(error?.message||'Не удалось сохранить путёвку');}finally{setSaveLoading(false);}};
  if(screen==='admin'&&profile?.role!=='admin')setScreen('home');
  if(screen==='waybills')return <WaybillList driverId={user.id} onBack={()=>setScreen('home')}/>;
  if(screen==='home')return <main className="app"><header><div><h1>Путёвка</h1><p>{profile?.full_name||user.email}</p></div><div className="header-actions">{profile?.role==='admin'&&<button className="icon" onClick={()=>setScreen('admin')}>⚙️</button>}<button className="icon" onClick={onLogout}>↪</button></div></header><section className="card profile"><span>👤</span><div><b>{profile?.role==='admin'?'Администратор':'Водитель'}</b><span>{profile?.full_name||user.email}</span></div></section><section className="card"><label>🚗 Автомобиль</label>{vehicleLoading?<p className="muted">Загрузка видов автомобилей...</p>:!vehicleTypes.length?<p className="muted">Виды автомобилей пока не добавлены.</p>:<select value={carId} onChange={e=>setCarId(e.target.value)}>{vehicleTypes.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select>}</section><button className="primary big" disabled={vehicleLoading||!car} onClick={()=>setScreen('waybill')}>＋ Новая путёвка</button><button className="card" style={{width:'100%',marginTop:12,cursor:'pointer'}} onClick={()=>setScreen('waybills')}>📋 Мои путёвки</button></main>;
  if(screen==='admin')return <Admin profile={profile} onBack={()=>setScreen('home')}/>;
  return <main className="app"><header><div><h1>Новая путёвка</h1><p>{car?.name||'Автомобиль'}</p></div><button className="icon" onClick={()=>setScreen('home')}>×</button></header><section className="card"><label>Одометр при выезде</label><div className="locked-field"><NumericInput value={odometer} onChange={e=>!odometerLocked&&setOdometer(e.target.value)} placeholder="125480" onEnter={()=>setOdometer(odometer.trim())}/><button className="small" onClick={()=>setOdometerLocked(!odometerLocked)}>{odometerLocked?'Изменить':'Зафиксировать'}</button></div><label>Начальный остаток, л</label><div className="locked-field"><NumericInput decimal value={startFuel} onChange={e=>!startFuelLocked&&setStartFuel(e.target.value)} placeholder="25" onEnter={()=>setStartFuel(startFuel.trim())}/><button className="small" onClick={()=>setStartFuelLocked(!startFuelLocked)}>{startFuelLocked?'Изменить':'Зафиксировать'}</button></div><div style={{fontSize:'12px',color:'#777',marginTop:'-4px',marginBottom:'4px'}}>Расход {car?.name||'автомобиля'}: город — {fmt(norm.city_consumption)} л/100 км · трасса — {fmt(norm.highway_consumption)} л/100 км</div></section><section className={`fuelbox ${lowFuel?'fuelbox-low':''}`}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',width:'100%'}}><span>{lowFuel&&<span className="warning">⚠️</span>} ⛽ Расчётный остаток</span><strong>{fmt(remaining)} л</strong></div>{Number(norm.city_consumption)>0&&remaining>0&&<em style={{display:'block',fontSize:'12px',color:'#777',marginTop:'4px',width:'100%'}}>ⓘ Хватит примерно на <b>{Math.round(remaining/Number(norm.city_consumption)*100)} км</b></em>}</section><section className="card"><div className="row"><h2>Поездки</h2><button className="small" onClick={()=>setShowTrip(true)}>＋ Добавить</button></div><TripList trips={trips} fmt={fmt} onDelete={deleteTrip}/>{!!trips.length&&<div className="totals">По поездкам: {fmt(tripKm)} км · Расход: {fmt(usedFuel)} л</div>}</section><section className="card"><div className="row"><h2>Заправки</h2><button className="small" onClick={()=>setShowRefuel(true)}>＋ Заправка</button></div>{!refuels.length?<p className="muted">Заправок не было.</p>:refuels.map((litres,i)=><div className="item" key={i}><div><b>⛽ Заправка АИ-92</b><span>Заправлено топлива</span></div><strong>+{fmt(litres)} л</strong></div>)}{!!refuels.length&&<div className="totals">Всего заправлено: {fmt(addedFuel)} л</div>}</section><button className="primary big" onClick={()=>{setSaveError('');setShowFinish(true)}}>✓ Сдать путёвку</button>{showTrip&&<div className="modal"><div className="modal-card"><div className="row"><h2>Новая поездка</h2><button className="icon" onClick={()=>setShowTrip(false)}>×</button></div><label>Город, км</label><NumericInput autoFocus placeholder="25 31" value={city} onChange={e=>setCity(e.target.value)} onEnter={addTrip}/><label>Трасса, км</label><NumericInput placeholder="120 80" value={highway} onChange={e=>setHighway(e.target.value)} onEnter={addTrip}/><div className="preview">Всего: <b>{fmt(cityKm+highwayKm)} км</b><br/>Расход: <b>{fmt(draftFuel)} л</b></div><button className="primary" onClick={addTrip}>Добавить поездку</button></div></div>}{showRefuel&&<div className="modal"><div className="modal-card"><div className="row"><h2>Заправка АИ-92</h2><button className="icon" onClick={()=>setShowRefuel(false)}>×</button></div><p>Сейчас в расчёте: <b>{fmt(remaining)} л</b></p><label>Заправлено, л</label><NumericInput autoFocus decimal placeholder="20" value={refuel} onChange={e=>setRefuel(e.target.value)} onEnter={addRefuel}/><div className="preview">После заправки: <b>{fmt(remaining+num(refuel))} л</b></div><button className="primary" onClick={addRefuel}>Сохранить заправку</button></div></div>}{showFinish&&<div className="modal"><div className="modal-card"><div className="row"><h2>Сдача путёвки</h2><button className="icon" onClick={()=>!saveLoading&&setShowFinish(false)}>×</button></div><div className="preview"><b>Итог:</b><br/>Начальный одометр: <b>{fmt(num(odometer))} км</b><br/>Пройдено: <b>{fmt(tripKm)} км</b><br/>Расчётный одометр: <b>{fmt(currentOdometer)} км</b><br/>Расход: <b>{fmt(usedFuel)} л</b><br/>Заправлено: <b>{fmt(addedFuel)} л</b><br/>Расчётный остаток: <b>{fmt(remaining)} л</b></div>{saveError&&<div className="error">{saveError}</div>}<button className="primary" disabled={saveLoading} onClick={finishWaybill}>{saveLoading?'Сохранение...':'Сохранить путёвку'}</button></div></div>}</main>;
}

function Root(){const[session,setSession]=useState(null),[profile,setProfile]=useState(null),[loading,setLoading]=useState(true);const loadProfile=async user=>{const{data}=await supabase.from('profiles').select('*').eq('id',user.id).single();setProfile(data||null);setSession(user)};useEffect(()=>{supabase.auth.getSession().then(({data})=>{if(data.session?.user)loadProfile(data.session.user);else setLoading(false)});const{data:listener}=supabase.auth.onAuthStateChange((_e,s)=>{if(s?.user)loadProfile(s.user);else{setSession(null);setProfile(null)}setLoading(false)});return()=>listener.subscription.unsubscribe()},[]);const logout=async()=>{await supabase.auth.signOut();setSession(null);setProfile(null)};if(loading)return <main className="app"><section className="card"><p>Загрузка...</p></section></main>;return session?<App user={session} profile={profile} onLogout={logout}/>:<Auth onLogin={u=>loadProfile(u)}/>}
createRoot(document.getElementById('root')).render(<Root />);